# 工單分流

一家旅遊電商的客服工單分流系統。系統讀進客戶來信，判斷這封信要不要處理、該由誰處理、客戶想要什麼，然後決定它可以自動回覆還是必須交給真人。

## 語彙

### 工單與分層

**工單**：
一封進線的客戶來信或網站表單，以 `ticket_id` 唯一識別。
_避免_：案件、信件、ticket

**受理判定**：
第一層判斷。這封信要不要進入客服流程。值為「受理」或「不受理」。
_避免_：處置、過濾、spam 判定

**承辦歸屬**：
第二層判斷。受理之後由誰承辦。值為「客服」或「轉其他單位」。
_避免_：軌、分類、部門

**服務情境**：
第三層判斷。客戶這封信要的是什麼。一封工單可以同時屬於多個服務情境。
_避免_：類別、意圖、主題、category

**情境群組**：
一組共用同一個後端查詢工具的服務情境。決定這個情境的下游要查訂單系統、查知識庫、還是什麼都不查。
_避免_：類型、大類

**客訴性質**：
一封工單是否帶有客訴情緒或求償訴求。它描述語氣與訴求強度，與服務情境正交，可以掛在任何情境上。
_避免_：客訴、抱怨、負面情緒

### 個資

**個資抽取**：
在本機辨識工單文字裡的個人資料。這個動作永遠在本機完成，抽取結果不離開本機。
_避免_：偵測、掃描、PII detection

**假名化**：
把辨識出的個資換成同型別的假值，例如把真實姓名換成另一個看起來一樣自然的姓名。假名化後的文字才允許送出本機。
_避免_：遮罩、去識別化、匿名化

**假名對照表**：
假值到原值的對照。只存在於單筆工單的處理過程中，不寫入任何檔案、日誌或外部請求。
_避免_：vault、mapping、還原表

**還原**：
在回覆送出前，把假值換回原值的程式化動作。
_避免_：解密、unmask

**殘留個資**：
假名化之後仍然留在文字裡的個資。它的存在代表抽取層漏抓了。
_避免_：漏網個資、未遮罩資料

### 分流

**分流**：
決定一封工單走自動處理還是交給真人。值為 `auto` 或 `human`。
_避免_：路由、派工、routing

**回覆型態**：
這封工單要送出什麼樣的回覆。與分流正交：一封工單可以既送出範本回覆、又同時交給真人接手。
_避免_：回覆方式、action

**不可逆動作**：
執行之後無法由系統自動撤回的動作：退款、變更訂單、開立文件、變更帳號。
_避免_：高風險動作、敏感操作

**轉真人**：
把工單交給客服人員處理。
_避免_：升級、escalate、human handoff

**降級**：
分類服務不可用或回應不合約定時，系統放棄判斷、一律轉真人的行為。
_避免_：fallback、容錯
